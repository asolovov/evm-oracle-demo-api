# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

.idea/

__debug_bin

# Service binary (matches APP in the Makefile)
evm-oracle-demo-api

# Generated protobuf outputs land under internal/genproto/ and are never
# committed (rule 9). The Dockerfile + Makefile regenerate them at build time.
internal/genproto/

# Local docker-compose data
/data/

# Local env files
.env
.env.local
